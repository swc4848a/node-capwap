var React = require('react');

var Main = React.createClass({
    render: function() {
        return (
            <div className="">
        		Hello Monitor!
        	</div>
        );
    }
});

module.exports = Main;
